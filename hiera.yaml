---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "os/%{facts.os.family}-%{facts.os.release.major}.yaml"
      - "kernel/%{facts.kernel}.yaml"
      - "common.yaml"
