---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "kernel/%{facts.kernel}.yaml"
      - "os_family/%{facts.os.family}.yaml"
      - "common.yaml"
